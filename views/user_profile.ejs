<link rel="stylesheet" href="/css/user_profile.css">

<img src="<%= profile_user.avatar %>" alt="<%= user.name %>" width="100px">

<!-- show the form only to the user who is logged in and viewing his profile page -->
    <% if(user.id == profile_user.id) {  %>
  <!-- if user matches,show the form -->
        <form action="/users/update/<%= profile_user.id%>" method="POST" enctype="multipart/form-data"> 
            <!-- You need to know which user to update hence we pass user's id as string param -->   
      
        <input type="text" name="name" placeholder="Your Name" value="<%= profile_user.name %>" required>
        <input type="email" name="email" placeholder="Your Email" value="<%= profile_user.email %>" required>
      
        <input type="file" name="avatar" placeholder="Profile picture">
        <input type="submit" value="update" >
        </form> 

    <% }else{ %>    
 <!-- else show only profile info -->
        <p> <%= profile_user.name %></p>
        <p> <%= profile_user.email%></p>
    <% } %>    